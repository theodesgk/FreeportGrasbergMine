#!/psw/cpp
geom	x:22:y:404:w:300:h:72
label	ÁÁ¶È ÿ 
action	Screen backlt:$1
getter	Screen backlt
/*
 * QVGA CGCs flicker badly if the backlight is lower than 40%.
 * Full VGA CGCs, however, need the full range.
 */
#if #arch(cgc)
#	if #arch(r2c) || #arch(r2l) || #arch(r3l) || #arch(r3c)
	min	40
	max	100
#	else
	min	0
	max	100
#	endif
#else
	min	0
	max	63
#endif
