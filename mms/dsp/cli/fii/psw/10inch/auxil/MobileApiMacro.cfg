#!/psw/cpp

#ifdef MOBILEAPI 
#	if (DEMAJOR == 6 && DEMINOR == 6 && DEMAINT >= 8) || (DEMAJOR > 6 || DEMINOR >= 7)
		macro	PublishOperatorMaterial	OperatorMaterialProvider publish:"$(matl.$(matl))";
#	else
		macro PublishOperatorMaterial	""
#	endif
#else
		macro PublishOperatorMaterial	""
#endif
