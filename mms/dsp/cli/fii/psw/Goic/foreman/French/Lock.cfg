#!/psw/cpp
geom	bw:4
hmin	100
map	n
cols    3
item	Pelleteuse...	req shvlck;menutype Shovels;menuselect menuactionob1;eq? "$(ob2)" "unlock" sendaction;eq? "$(ob2)" "lock" Shovels action:menuaction post
item	Benne...		req dmplck;menutype Locs;menuselect actlocmenu;eq? "$(ob2)" "unlock" sendaction;eq? "$(ob2)" "lock" Locs action:menuaction post
item	Région...	req reglck;menutype Region;menuselect menuactionob1;eq? "$(ob2)" "unlock" sendaction;eq? "$(ob2)" "lock" Region action:menuaction post

item    Quitter              Lock unpost
#if #arch(cgc)
itemColor Quitter:Exit
#endif
