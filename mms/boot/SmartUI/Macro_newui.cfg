#!/psw/cpp

#ifdef PORTRAIT
macro initSmartUI	w0.Emerg map:n;w0.Clock label:'>>';w0.Clock action:StatBarRollR;SmartUIinitextra
#else
macro initSmartUI	w0.Emerg map:n;w0.Clock label:'';w0.Clock action:'';SmartUIinitextra
#endif 
/* redefine GetTime1 to prevent it from displaying anything when it is called from WinMgr init */
macro GetTime1 SmartUI 1
