#!/psw/cpp

#ifdef MOBILEAPI 
#	if (DEMAJOR == 6 && DEMINOR == 6 && DEMAINT >= 8) || (DEMAJOR > 6 || DEMINOR >= 7)
		macro PublishUser               UserProvider publish:$3:$7
		macro PublishEquipmentName      EquipmentInformationProvider publish:"$(OMS host)"
		macro PublishOperatingState	OperatingStateChangeProvider publish:$1:$2:$4
#	else
		macro PublishUser               ""
		macro PublishEquipmentName	""
		macro PublishOperatingState	""
#	endif
#else
	macro PublishUser               ""
	macro PublishEquipmentName	""
	macro PublishOperatingState	""
#endif
