%!/psw/cpp
/*
 * * * * * * * * * * * * * * * * * * *
 *  DISPATCH Real-Time Firmware Module
 * Copyright (c) Modular Mining Systems, 1982-1995
 *               All Rights Reserved
 *  $RCSfile: Main.cfg,v $
 * $Revision: 1.16 $
 *   $Author: olivas $
 *     $Date: 2019/08/05 19:32:10 $
 *    $State: Exp $
 *   Purpose: Main configuration file for GOIC application.
 * * * * * * * * * * * * * * * * * * *
 */
#if !#arch(pe)
#	include <Version.h>
#	include <Main/UpPort.h>
#	if #arch(cgc)
		version	CGC, vVERSION
#		define PROMPT \'CLASS CGC, vVERSION> \'
#	else
		version	Goic, vVERSION
#		define PROMPT \'CLASS Goic, vVERSION> \'
#	endif
#	include <Main/ObjSh.h>
#	include <Main/Adc.h>
#	include <Main/OMS.h>
	module	File
#	if !#up(Slip)
		module	Slip
		object	Slip
#	endif
#endif /* #arch(pe) */

module	Gui
object	Screen
#if #arch(r2k)
	object	Dse speed:9600 hdwe:rs232 port:cpu3
#endif
object	Tp
#ifndef DELITE
	object	InfoPanel
#	if #option(Elevation)
		object  Elevation
#	endif
#	if DEMAJOR > 6 | (defined DEMAINT && DEMAINT >= 3)
		object QuickMessage
#	endif
#elif defined DESELECT
#	if #option(Elevation)
		object  Elevation
#	endif
#endif
object	Keyboard
object	Macro path:y
object	WinMgr
#if (DEMAJOR == 6 && DEMINOR >= 5) || DEMAJOR > 6
#	if defined DELITE
		object VolumeControl
#	endif
#endif

#if defined MOBILEAPI
#if	(DEMAJOR == 6 && DEMINOR == 6 && DEMAINT >= 8) || (DEMAJOR > 6 || DEMINOR >= 7)
		module MobileApiProvider
		object UserProvider 
		object EquipmentInformationProvider 
#		if #class(excav)
			object OperatorMaterialProvider
			object OperatingStateChangeProvider
			object ShovelBucketMeasurementConsumer
			object FinalExcavatorPayloadConsumer
#		endif
#	endif
#endif

#if defined TRUCKEVENTMANAGER 
#if	(DEMAJOR == 6 && DEMINOR == 6 && DEMAINT >= 8) || (DEMAJOR > 6 || DEMINOR >= 7)
#		if #class(excav)
			module TruckEventManager
			object TruckEventManager
#		endif
#	endif
#endif

#define OBJECT Main
#include <Cfg/Options.cfg>
