#!/psw/cpp

#if #arch(pe.net)
/* Port Embedded Hub and Goic share the same Macro space so we don't need to send velocity to goic 
 * This macro will be redefined in Macro.cfg on goic side
 */
macro sendvel gpsVel

#else
/* Send the GPS velocity to the GC */
out     SENDVEL 0x1974  d       sendto Goic
macro   sendvel SENDVEL $(GpsTruck velocity)
#endif
