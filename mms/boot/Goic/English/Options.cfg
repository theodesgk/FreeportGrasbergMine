#!/psw/cpp
geom	bw:4
hmin	92
wmin	150
map	n
cols	3
item    Recall\nCommand         kpRecall
item    Refresh\ Screen         WinMgr refresh
item    Clear\ Text\nWindow     w0.Text clear
/* item    Hardware\ Test          TestInstall post  */
item	Message	kpMessage
#if #arch(pe.net)
item	Lock\ Screen		Screen lock
#endif
#define OBJECT  Options
#include <Cfg/Options.cfg>
item	Exit\ Menu		Options unpost
#if #arch(cgc) || #arch(pe)
itemColor Exit\ Menu:Exit
#endif
