macro	SetMaterial	req matl;ob1 $(matl);menutype Material;args 2;menuselect matmenuact;menuaction

macro   matmenuact      id $1;preMatl
macro   preMatl         matl.$(matl) $(id);SetMatl;sendaction

symbol	trkload	y
symbol	chngloc	y

macro	kpChngLoc	eq? "$(chngloc)" "n" notavail;\
			ne? "$(chngloc)" "n" doChngloc

macro	doChngloc	args 1;req setloc;typ L;menutype Locs;menuselect menuaction5;Locs action:menuaction post
macro	notavail	Ok label:$(msg.notavail) post

symbol	kpaction
symbol	kplabel

symbol unit.1    Truck
symbol  icon.7   ready
symbol  icon.3   standby
symbol  icon.8   loading
symbol  icon.9   full
symbol  icon.13  waiting
symbol  icon.16  tramloading
symbol  icon.21  down
symbol  icon.22  standby
symbol  icon.23  delay
symbol  icon.24  tiedown
symbol  icon.25  ready
macro   rpc.7    kpPanFull
macro   rpc.21   kpShvReady
macro   rpc.22   kpShvReady
macro   rpc.23   kpShvReady
macro   rpc.24   kpShvReady
macro   rpc.25   kpPanFull
message msg.21   Down at $(loc)
message msg.22   Standby at $(loc)
message msg.23   Delayed at $(loc)
message msg.24   Parkup at $(loc)
message msg.25   Waiting for Truck at $(loc)
message msg.16   Loading $(unit.$(u1)) $(id1)
message msg.7    Waiting for Truck at $(loc)

macro	kpIdle

macro	postunitmenu	menutype $(typ)Unit$(unum);menuselect menuactionid;$(typ)Unit$(unum) action:menuaction post

macro kpLogOn kplabel $(cfgLbl.68);kpaction 'DoLogon';Kp label:$(kplabel) post
macro kpFuel          kplabel $(cfgLbl.69);kpaction 'sendfuel';Kp label:$(kplabel) post
