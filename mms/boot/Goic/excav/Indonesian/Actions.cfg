#!/psw/cpp
geom	x:0:y:0:bw:4
hmin	120
wmin	120
map	n
cols	4
tile	slash
item	Log\ On...	        kpLogOn
item	Log\ Off...	        kpLogOff
#if #class(loader) || #class(lhd) || #class(ug_loader) || #class(loaderlp)
item	Tiba			kpArrive
item	Tugas			kpAssign
item	Beban			kpLoad
#endif
item	Penuh	                kpFull
#if #class(excav) || #class(shovel) || #class(loader) || #class(lhd) || #class(ug_loader) || #class(loaderlp)
item	Setel\n Lokasi\ Beban...  kpChngLoc   
item	Setel\n Pemuatan\ Truk...	kpTrkLoad
#endif
#if #class(loader) || #class(lhd) || #class(ug_loader) || #class(loaderlp)
item	Bahan\ Bakar...	                kpFuel
item	Ubah\nModus\nTramming	kpTrammode
#endif
#if #class(shovel) || #class(loader)
item	Split\nDigging		WinMgr window:wSplits
#endif
item	Checklist...	kpPrestart
item	Ya	                kpYes
item	Tidak	                kpNo
#define OBJECT  Actions
#include <Cfg/Options.cfg>
item	Keluar\ Menu	        Actions unpost
item	Darurat	        kpHelp
#if #arch(cgc) || #arch(pe)
itemColor	Ya:Yes
itemColor	Tidak:No
itemColor       Darurat:Down
itemColor	Keluar\ Menu:Exit
#endif
