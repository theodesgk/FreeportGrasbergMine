%!/psw/cpp

#if #inclinometer(vsms)
	source	Vsms
#endif

#ifdef MOUNT
	mount	MOUNT
#endif

#if #arch(hub)
	sendTo	Master.-1
#elif #arch(gsp)
	sendTo	Loop.-1
#elif #arch(mlink) && #option(MineCare/RoadRoughness)
	sendTo	192.168.201.0
#else
	sendTo	Can.-1
#endif

